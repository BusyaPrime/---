# Математический Вывод: Устранение Ghost-Ячеек (Ghost Cell Elimination) 2-го Порядка для Границ Неймана и Робина

Чтобы железобетонно удержать второй порядок $O(h^2)$ сходимости по пространству по всему домену, граничные условия тоже обязаны дискретизироваться минимум с точностью $O(h^2)$. Любимые грабли джунов — лепить на границе разности 1-го порядка (типа $(u_0 - u_1)/h$), что тупо роняет глобальную сходимость всего солвера до $O(h)$. 🤓

PDE-LAB юзает хардкорное **Устранение Ghost-ячеек (Ghost Cell Elimination)** через центральные разности как для Неймана, так и для Робина.

## 1. Домен и Призрачные Узлы (Ghost Nodes)
Возьмем 1D срез домена $x_i$, где физическая граница сидит ровно в узле $x_0$. 
Внутренние (interior) узлы — это $x_1, x_2, \dots$.
Вводим виртуальный "ghost" узел $x_{-1}$, который лежит на расстоянии $x_0 - h$, то есть тупо за пределами нашего реального массива.

Формула центральной разности (central difference) Лапласиана на граничном узле $x_0$ выглядит так:
$$ \nabla^2 u(x_0) \approx \frac{u_{-1} - 2u_0 + u_1}{h^2} $$

Так как $x_{-1}$ — это фантом вне физического массива, его надо алгебраически выпилить (eliminated) с помощью математики граничного условия.

## 2. Граничное Условие Неймана (Neumann Boundary Condition)
Нейман задает поток производной (derivative flux) на границе:
$$ \frac{\partial u}{\partial n}\Big|_{x_0} = g $$

Юзаем центральную разность 2-го порядка для первой производной в узле $x_0$:
$$ \frac{u_1 - u_{-1}}{2h} = g \implies u_{-1} = u_1 - 2hg $$

Подставляем $u_{-1}$ обратно в Лапласиан в точке $x_0$:
$$ \nabla^2 u(x_0) \approx \frac{(u_1 - 2hg) - 2u_0 + u_1}{h^2} = \frac{2u_1 - 2u_0}{h^2} - \frac{2g}{h} $$

**Как это реализовано в PDE-LAB под капотом:**
В `BoundaryOperator.java` мы накатываем это влияние прямиком в формулу внутреннего стенсила (interior stencil). Физический узел $x_0$ теперь жрет коэффициент $2 \times u_1$ вместо дефолтного $u_1$, а вектор форсинга (forcing term) ловит в свой RHS законный довесок потока $-2g/h$. Никаких дополнительных IF-ов в хот-лупе.

## 3. Граничное Условие Робина (Robin / Convective / Mixed)
Робин задает линейную комбинацию самого значения и его нормальной производной (классика для конвективного теплообмена):
$$ -\kappa \frac{\partial u}{\partial n}\Big|_{x_0} = H (u_0 - u_{\infty}) $$
Пусть $g \equiv \frac{H}{\kappa}$, тогда:
$$ -\frac{\partial u}{\partial n}\Big|_{x_0} = g(u_0 - u_{\infty}) $$

Снова врубаем центральную разность 2-го порядка:
$$ -\frac{u_1 - u_{-1}}{2h} = g(u_0 - u_{\infty}) \implies u_{-1} = u_1 + 2hg(u_0 - u_{\infty}) $$

Подставляем всё это добро обратно в Лапласиан:
$$ \nabla^2 u(x_0) \approx \frac{(u_1 + 2hg(u_0 - u_{\infty})) - 2u_0 + u_1}{h^2} = \frac{2u_1 - 2u_0 + 2hgu_0}{h^2} - \frac{2hgu_{\infty}}{h^2} $$
$$ \nabla^2 u(x_0) \approx \frac{2u_1 - (2 - 2hg)u_0}{h^2} - \frac{2g u_{\infty}}{h} $$

**Итого (Conclusion):** 🤓
Алгебраически аннигилируя фантомный узел ДО сборки матрицы или эвалюации правой части (RHS), стенсил сохраняет идеально симметричный перф на $x_0$, юзая только легальную память внутренних узлов $u_1, u_0$. Эта изящная магия цементирует теоретическую глобальную асимптотическую сходимость $O(h^2)$, что безжалостно пруфается нашими `MMS` тестами, которые всегда выплевывают лог-лог наклон ровно в $-2.0$.
