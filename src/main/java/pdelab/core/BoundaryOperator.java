package pdelab.core;

/**
 * Интерфейс для инжекта механик граничных условий (шоб без боли).
 * Жестко изолирует логику границ от кор-дискретизации
 * и по красоте хэндлит инжекты Дирихле, Неймана и Робина.
 */
public interface BoundaryOperator {

    /**
     * Мутируем краевые ноды uFull с оглядкой на время t.
     */
    void apply(Grid2D grid, double[] uFull, double t);
}
