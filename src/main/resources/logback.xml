<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Console Appender config specifically for the application, not libraries -->
    <logger name="pdelab" level="INFO" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>

    <root level="OFF">
        <!-- Disable root level to prevent noisy output from dependencies -->
    </root>
</configuration>
